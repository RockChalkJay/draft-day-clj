pom.xml
pom.xml.asc
*.jar
*.class
/lib/
/classes/
/target/
/checkouts/
.lein-deps-sum
.lein-repl-history
.lein-plugins/
.lein-failures
.nrepl-port
.cpcache/

.vscode/
.calva/
.clj-kondo/
.cljs_node_repl/
.lsp/
.dir-locals.el
out/

.claude/

# node / shadow-cljs
/node_modules/
/.shadow-cljs/
# compiled CLJS output (generated by shadow-cljs)
/resources/public/js/

# local data cache (bundled sample is committed). Globbed: the filename carries
# the row schema version, so a bump orphans the old file rather than reusing it.
/data/players_cache*.transit
# research-harness caches: derived, re-fetchable, and large (~100MB once a
# benchmark sweep has pulled every season from every source)
/data/benchmark_cache/
/data/replay_cache/
